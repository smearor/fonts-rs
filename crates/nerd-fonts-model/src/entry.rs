//! Metadata entry for a single exported Nerd Font icon.
//!
//! `IconEntry` is now a type alias for `GlyphEntry<IconName>`, defined in
//! [`crate::lib`]. This module re-exports it for backward compatibility.

pub use crate::IconEntry;
